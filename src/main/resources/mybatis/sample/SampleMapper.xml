<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootjsp.sample.mapper.SampleMapper">

    <!-- 매물시퀀스조회 -->
    <select id="selectUserNm" resultType="string">
        SELECT USER_NM
        FROM dbo.CMN_ADMIN_MNG
    </select>

</mapper>